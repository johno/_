---
title: Tomato
slug: /tomato
date: 2020-03-19
tags:
  - css
  - design
  - color
---

export const Flex = props => (
  <div
    style={{
      display: 'flex',
      alignItems: 'center',
      marginTop: 20
    }}
    {...props}
  />
)

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, maxWidth: 200, backgroundColor: 'tomato', marginRight: 20 }} />

## I think that tomato is the best named CSS color.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, flexGrow: 1, backgroundColor: 'black', color: 'tomato', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}>AAA</div>

When combined with `black`, `tomato` is accessible. It results in a
**AAA** score and the contrast ratio is **7.1**.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, flexGrow: 1, color: 'tomato', border: '2px solid black', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}>F</div>

When combined with `white`, `tomato` isn't accessible. It results in an
**F** score and the contrast ratio is **2.9**.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, flexGrow: 1, border: '16px solid tomato', backgroundColor: 'black', color: 'tomato', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><code>#</code></div>

In hexadecimal format, tomato is represented as `#FF6347`.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, flexGrow: 1, backgroundColor: 'tomato', color: 'black', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><code>hsl</code></div>

In hsl format, tomato is represented as `hsl(9.1, 100%, 63.9%)`.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, flexGrow: 1, border: '8px solid tomato', color: 'black', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><code>rgb</code></div>

In rgb format, tomato is represented as `rgb(255, 99, 71)`.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, maxWidth: 200, flexGrow: 1, color: 'tomato', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><span aria-label="tomato">🍅</span></div>

There's an emoji for tomato.

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, maxWidth: 200, border: '32px solid black', flexGrow: 1, color: 'tomato', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><code>{'{}'}</code></div>

<div>

You can create a CSS class to make things `tomato`.

```css
.tomato {
  color: tomato;
}
```

</div>

</Flex>

<Flex>

<div style={{ height: 200, width: 200, minWidth: 200, maxWidth: 200, flexGrow: 1, color: 'tomato', fontWeight: 'bold', display: 'flex', alignItems: 'center', justifyContent: 'space-around', lineHeight: 1, fontSize: 80, marginRight: 20 }}><span aria-label="tomato">🥬</span></div>

Tomato is technically a fruit, but is often mistaken for a vegetable.
This makes it twice as healthy for you.

</Flex>

<div style={{ backgroundColor: 'tomato', height: '100vh', width: '100vw', marginLeft: 'calc(-100% + 50vw - 27px)', marginBottom: 80 }} />

## What's your favorite named color?

Let me know on [Twitter](https://twitter.com/4lpine)
