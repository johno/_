{
  "private": true,
  "name": "johno.com",
  "scripts": {
    "start": "gatsby develop",
    "build": "gatsby build",
    "deploy": "now switch johno && now && now alias $(pbpaste) johno.com && now alias $(pbpaste) www.johno.com",
    "now-build": "yarn build"
  },
  "dependencies": {
    "gatsby": "^2.3.15",
    "gatsby-theme-digital-garden": "^0.0.16",
    "gatsby-theme-digital-garden-blog": "^0.0.15",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-three-fiber": "^2.0.4",
    "three": "^0.103.0"
  }
}
