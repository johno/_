import Layout from '../components/layout'
export default (props) => (
  <Layout skipSubscribe={true} skipTitle={true} {...props} />
)

# BlackLivesMatter

[Donate (preferably monthly) &rarr;](https://blacklivesmatters.carrd.co/#donate)
